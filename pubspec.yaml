name: meeting_app
description: "A Flutter app for managing meetings and schedules."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.1.2  # แก้จาก 6.1.2 เป็น 6.1.5

  # Database
  sqflite: ^2.4.1   # แก้จาก 2.3.3+1 เป็น 2.3.2
  path_provider: ^2.1.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Date & Time
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0  # แก้จาก 5.0.0 เป็น 3.0.0

flutter:
  uses-material-design: true

  # # ไฟล์รูปภาพ
  # assets:
  #   - assets/images/

  # # ฟอนต์
  # fonts:
  #   - family: NotoSans
  #     fonts:
  #       - asset: assets/fonts/NotoSans-Regular.ttf
  #       - asset: assets/fonts/NotoSans-Bold.ttf
  #         weight: 700
